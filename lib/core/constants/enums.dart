enum MessageState { read, delivered, none }
enum UserEngagementState { typing, online, last_seen, none }
enum ChannelViewState { initial, loading, loaded, error }
enum ChannelListTabState { all, unread }
enum MessagePosition { continuous, normal }
enum PopupMenuType { edit, delete, copy }
